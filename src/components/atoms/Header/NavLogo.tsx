import styled from "styled-components";

const NavLogo = () => {
  return <NavLogoStyled src="/Logo.png" />;
};

const NavLogoStyled = styled.img`
  width: 64px;
  margin: auto;
`;

export default NavLogo;
